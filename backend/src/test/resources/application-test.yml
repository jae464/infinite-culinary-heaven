spring:
  config:
    activate:
      on-profile: "test"
  datasource:
    url: "jdbc:h2:mem:test;MODE=MYSQL;NON_KEYWORDS=USER"
  jpa:
    defer-datasource-initialization: true
image:
  store:
    dir: "${user.dir}/images"